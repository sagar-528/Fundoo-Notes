import React, { Component } from 'react'
import { View, Text } from 'react-native'

class Reminder extends Component {

    constructor(props) {
        super(props)
    }
    
    render(){
    return (
        <View>
            <Text> Reminder Component </Text>
        </View>
    )}
}

export default Reminder
